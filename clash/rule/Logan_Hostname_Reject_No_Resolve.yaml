payload:
  - DOMAIN,e.crashlytics.com
  # 防止 YouTube 等使用 QUIC 导致速度不佳, 禁用 443 端口 UDP 流量(不包括国内)
  - AND, ((DOMAIN-SUFFIX, googlevideo.com), (DST-PORT,443),(NETWORK,UDP))
  - AND, ((DOMAIN-SUFFIX, youtubei.googleapis.com), (DST-PORT,443),(NETWORK,UDP))
